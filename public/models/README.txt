Đặt file mẫu GLB/GLTF tại đây (ví dụ: sample.glb).
Trong app: Thuộc tính → Load model → "Thử /models/sample.glb" hoặc "Chọn file...".

Collider vật lý vẫn dùng hình học đơn giản (box/sphere); model chỉ thay lớp hiển thị.
